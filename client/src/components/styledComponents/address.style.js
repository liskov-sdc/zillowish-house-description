import styled from 'styled-components';

const Street = styled.div`
  font-size: 33px;
  line-height: 1.3;
  font-weight: 700;
`;

const CityStateZip = styled.div`
  font-size: 28px;
  line-height: 1.3;
  font-weight: 700;
`;

export default {Street, CityStateZip}